// Here we manually create a hisory object to have
// easier access to it
import { createBrowserHistory } from 'history';
// React Router has already intalled package history

export default createBrowserHistory();
